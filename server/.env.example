# Example environment variables for development
# Copy this file to .env and fill in your values

# Secret key for Flask sessions (change this in production!)
SECRET_KEY=dev-secret-key-change-in-production

# Database URL (default is SQLite, change to PostgreSQL for production)
# SQLite (default):
# DATABASE_URL=sqlite:///tricab.db
# PostgreSQL example:
# DATABASE_URL=postgresql://username:password@localhost/tricab

# OpenAI API Key (optional - for AI features)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
